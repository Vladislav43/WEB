<!DOCTYPE html>
<html>
<head>
    <title>Chat Bot</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
            margin: 0;
            padding: 20px;
        }

        h1 {
            text-align: center;
            color: #333333;
        }

        #chat-container {
            background-color: #ffffff;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        #chat-form {
            text-align: center;
        }

        #user-input {
            width: 300px;
            padding: 10px;
            font-size: 14px;
            border: 1px solid #cccccc;
            border-radius: 5px;
        }

        #user-input:focus {
            outline: none;
            border-color: #999999;
        }

        #send-button {
            padding: 10px 20px;
            font-size: 14px;
            border: none;
            background-color: #007bff;
            color: #ffffff;
            border-radius: 5px;
            cursor: pointer;
        }

        #send-button:hover {
            background-color: #0056b3;
        }

        .message {
            margin-bottom: 10px;
            padding: 10px;
            border-radius: 5px;
            background-color: #f9f9f9;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
        }

        .user-message {
            text-align: right;
            background-color: #e6f2ff;
        }

        .bot-message {
            text-align: left;
            background-color: #f9f9f9;
        }

        .message-content {
            margin-bottom: 5px;
        }

        .timestamp {
            font-size: 12px;
            color: #999999;
            text-align: right;
        }
    </style>
</head>
<body>
    <h1>Chat Bot</h1>

    <div id="chat-container"></div>

    <form id="chat-form">
        <input type="text" id="user-input" placeholder="Задайте ваше питання" />
        <button id="send-button" type="submit"><i class="fas fa-paper-plane"></i> Надіслати</button>
    </form>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/js/script_2.js"></script>
</body>
</html>
